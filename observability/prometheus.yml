global:
  scrape_interval: 15s

scrape_configs:
  - job_name: sinsay-app
    scrape_interval: 15s
    metrics_path: /actuator/prometheus
    basic_auth:
      username: ${ACTUATOR_USER}
      password: ${ACTUATOR_PASSWORD}
    static_configs:
      - targets: ['app:8080']
