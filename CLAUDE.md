# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

## Claude Code — Additional Instructions

- Commit after every meaningful step before moving to the next.
- Before committing: verify `./gradlew test` passes.
- Use Context7 MCP libraries for up-to-date documentation:
  - `/jetbrains/kotlin` — Kotlin
  - `/websites/spring_io_projects_spring-ai` — Spring AI
  - `/spring-projects/spring-boot` — Spring Boot
  - `/kotlin/kotlinx.html` — Kotlin HTML DSL
  - `/websites/htmx` — HTMX
  - `/gradle/gradle` — Gradle